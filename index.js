const record = [
    { year: "2015", result: "W"},
    { year: "2014", result: "N/A"},
    { year: "2013", result: "L"},
  ]
  function superbowlWin(){
   return ('2015')
  }
    let recordList = record.find(record=>{
        return (record.year.superbowlWin) 
    })
    
console.log(recordList)